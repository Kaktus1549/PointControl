services:
  ssh:
    container_name: airsport-hiddencat
    build: .
    ports:
      - 2223:22 
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: "128M"
    restart: unless-stopped
